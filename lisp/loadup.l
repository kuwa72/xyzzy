;;; -*-Lisp-*-
;;;
;;; This file is part of xyzzy.
;;;

;; Core CL libraries (platform-independent)
(si:*load-library "evalmacs") (gc)
(si:*load-library "package") (gc)
(si:*load-library "list") (gc)
(si:*load-library "setf") (gc)
(si:*load-library "typespec") (gc)
(si:*load-library "array") (gc)
(si:*load-library "number") (gc)
(si:*load-library "handler") (gc)
(si:*load-library "stream") (gc)
(si:*load-library "sequence") (gc)
(si:*load-library "charname") (gc)
(si:*load-library "condition") (gc)
(si:*load-library "hash") (gc)
(si:*load-library "misc") (gc)
(si:*load-library "ldefs") (gc)
(si:*load-library "help") (gc)
(si:*load-library "builtin") (gc)
#-ncurses (si:*load-library "estartup") (gc)
(si:*load-library "encoding") (gc)
(si:*load-library "keyboard") (gc)
(si:*load-library "keymap") (gc)
(si:*load-library "defs") (gc)
(si:*load-library "cmds") (gc)
(si:*load-library "abbrev") (gc)
(si:*load-library "buffer") (gc)
#-ncurses (si:*load-library "window") (gc)
#-ncurses (si:*load-library "font") (gc)
(si:*load-library "kanji") (gc)
(si:*load-library "page") (gc)
(si:*load-library "region") (gc)
(si:*load-library "rectangl") (gc)
(si:*load-library "paragrph") (gc)
(si:*load-library "sexp") (gc)
(si:*load-library "fill") (gc)
(si:*load-library "files") (gc)
(si:*load-library "complete") (gc)
(si:*load-library "minibuf") (gc)
(si:*load-library "history") (gc)
(si:*load-library "kbdmacro") (gc)
#-ncurses (si:*load-library "timestmp") (gc)
#-ncurses (si:*load-library "filer") (gc)
(si:*load-library "search") (gc)
#-ncurses (si:*load-library "process") (gc)
#-ncurses (si:*load-library "register") (gc)
#-ncurses (si:*load-library "select") (gc)
#-ncurses (si:*load-library "menu") (gc)
#-ncurses (si:*load-library "app-menu") (gc)
#-ncurses (si:*load-library "mouse") (gc)
#-ncurses (si:*load-library "gmark") (gc)
#-ncurses (si:*load-library "tags") (gc)
#-ncurses (si:*load-library "kwd") (gc)
#-ncurses (si:*load-library "re-kwd") (gc)
#-ncurses (si:*load-library "lispmode") (gc)
#-ncurses (si:*load-library "c-mode") (gc)
#-ncurses (si:*load-library "cc-mode") (gc)
#-ncurses (si:*load-library "textmode") (gc)
#-ncurses (si:*load-library "errors") (gc)
#-ncurses (si:*load-library "grep") (gc)
#-ncurses (si:*load-library "buf-menu") (gc)
#-ncurses (si:*load-library "optprop") (gc)
#-ncurses (si:*load-library "loptprop") (gc)
#-ncurses (si:*load-library "winhelp") (gc)
#-ncurses (si:*load-library "edict") (gc)
#-ncurses (si:*load-library "paths") (gc)
#-ncurses (si:*load-library "monkey") (gc)
#-ncurses (si:*load-library "encdec") (gc)
#-ncurses (si:*load-library "boxdraw") (gc)
#-ncurses (si:*load-library "cmdbar") (gc)
#-ncurses (si:*load-library "pframe") (gc)
#-ncurses (si:*load-library "session") (gc)
#-ncurses (si:*load-library "about") (gc)
(si:*load-library "common-lisp") (gc)
#-ncurses (si:*load-library "siteinit" :if-does-not-exist nil) (gc)
