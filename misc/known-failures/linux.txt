# Known failures on the native POSIX build (linux.yml), run as
# _build/linux/xyzzy --batch.  See README.md.
#
# **This list is about the frontend not being finished, not about a toolchain
# difference.**  misc/known-failures/mingw.txt holds "MSVC passes, mingw does
# not"; everything here is "Windows passes, POSIX has no implementation yet".
# So a line coming off this list is progress on src/frontend/ncurses and
# src/core/vfs-posix.cc, and #50 is where the missing pieces are listed.
#
# Tests that take the *process* down do not belong here -- a dead process ends
# the run and everything after it goes unmeasured.  Those go in the exclude list
# in tools/run-tests.sh, with the reason.
#
# Baseline: main @ a63a907 で 158 件。以降 1 件ずつ減っている
# (redefun-docstring と text-mode-does-not-save-buffer-package は issue #105 で
# 直したので外した)。
#
# **「理由が分からない」群は空になった。** 4 件あったうち 2 件は本物の移植バグ
# (上の 2 件)、残る 2 件は分類の間違いだった: 循環リストの印字はサブプロセス群、
# `abbreviate-display-string` は mingw でも落ちるので POSIX 固有ではない。
# 理由の書けない名前を置いたままにしないこと。
#
# 内訳の数はグループの見出しの下に並んでいる名前を数えれば分かる。1 つでも
# 通るようになったら run が赤くなるので、そのとき消す。

# FFI: DLL / 共有ライブラリを読み、その関数を呼ぶ。
#
# **POSIX でも呼べるようになった (#133 の段階 2〜3)。** 前はここに
# 「dlopen へ載せ替えれば大半は動くはずだが、stdcall と可変長引数の扱いは
# 別に考える必要がある」と書いてあり、**libffi を入れるかどうかの判断待ちに
# していたが、その判断は要らなかった。**
#
# `src/frontend/win32/dll.cc` の x86_64 の枝は、アセンブリでも libffi でも
# なく**関数ポインタのキャスト**で呼んでいる (`int64_t (*)(int64_t, ...)` へ
# キャストして呼ぶと、**呼び出し規約はコンパイラが出す**)。同じ形を
# src/core/dll-posix.cc に置いた。POSIX x86_64 / aarch64 には規約が 1 つしか
# 無いので、stdcall の区別も要らない。
#
# 実測 (glibc): `abs(-5)` = 5、`getpid()` が `si:getpid` と一致、
# `atoi("42")` = 42、`llabs(-8589934592)` = 8589934592、`strchr` がポインタを
# 返す。`unittest/ffi-portable-tests.l` に 9 件 (**両方のプラットフォームで
# 同じテストになる**)。
#
# **ここに残っているのは 2 種類だけ:**
#
#   * `c-callable-*` 2 件 — **Lisp の関数を C から呼べるアドレスにする**もの。
#     実行時に機械語を作る必要がある (`lc_callable::insn`)。ABI ごとに書くか
#     libffi の closure を使うかの判断が残っている (#133 の段階 4)。
#   * それ以外 — **テストが MSVC のランタイム固有の関数を呼んでいる。**
#     `_itoa` / `_atoi64` / `_i64toa` / `_ui64toa` は msvcrt にしかない。
#     **実装の不足ではない** (同じことを標準 C の関数でやる
#     ffi-portable-tests.l は POSIX でも通る)。
#
# `fix-for-ffi-c-function-return-doubl/float-00` は返り値が double のもので、
# 返り値だけなら POSIX でも通る形に書いてある。**手元の run では
# `.lc` が無いと落ちるが CI では通る**ので、ここに載っているのは別の理由。
#
# **`pack-unpack-*` 4 件はここから外れた。FFI とは関係が無かった。**
# `si:pack-*` / `si:unpack-*` はチャンク (バイト列) を読み書きするだけで DLL を
# 触らない。落ちていたのは core のバグ 2 つで、どちらも `long` の幅:
#
#   * `unpack-int32` が `long` で読んでいた。LP64 では 64bit なので**4 バイトの
#     欄から 8 バイト読み**、32bit からの符号拡張も起きない
#     (0xFFFFFFFF を pack して読むと -1 でなく 4294967295)。固定幅の
#     `int32_t` などに直した。`char` も符号の有無が処理系任せで、**Linux の
#     ARM では符号無し**なので `unpack-int8` が負を返せない。
#   * `make_integer (u_long)` が `int64_t` へ落としていた。LP64 では
#     `make_integer (uint64_t)` が `#if ULONG_MAX != UINT64_MAX` の中にあって
#     **そもそもコンパイルされない**ので、**LONG_MAX を超える符号無し 64bit を
#     一切作れなかった** (全ビット 1 が -1 になる)。
#
# **群の名前を信じて中を見ないと、こういうものが混ざったまま残る。**
# ここに残っているのは本当に LoadLibrary / GetProcAddress / stdcall が要るもの
# だけか、足すときに確かめること。

c-callable-check-type-1
c-callable-typep
c-vaargs-invalid-vaargs-0
define-dll-entry-int32-arg
define-dll-entry-int64-arg
define-dll-entry-return-int32
define-dll-entry-return-int64
define-dll-entry-return-uint32
define-dll-entry-return-uint64
define-dll-entry-uint32-arg
define-dll-entry-uint64-arg
defun-c-callable-cdecl
defun-c-callable-stdcall
fix-for-ffi-c-function-return-doubl/float-00
funcall-expand-vaargs
funcall-invalid-c-args
funcall-invalid-vaargs-1
funcall-invalid-vaargs-2
funcall-invalid-vaargs-3
funcall-invalid-vaargs-4
funcall-invalid-vaargs-5
funcall-invalid-vaargs-6
funcall-invalid-vaargs-7
funcall-vaargs
funcall-vaargs-2
funcall-vaargs-default-argument-promotions
funcall-vaargs-noarg
make-c-function-stdcall-rest

# Win32 API を直接呼ぶもの。
# 
# `last-win32-error` 2 件は **GetLastError ではなく FFI で止まっている。**
# どちらも `MultiByteToWideChar` を DLL 経由で呼んでいて (foreign-test.l)、
# 呼ぶ側が無い (#133)。上の FFI 群と同じ理由なので、そちらが動けば一緒に
# 通るはず。`wait-object` は WaitForMultipleObjects。
#
# **`machine-type` はここから外れた。** 「GetSystemInfo」と書いてあったが、
# **POSIX でも `machine-type` は値を返していた** ("amd64")。落ちていたのは
# テストの期待値が `#+x86 "x86" #+x64 "x64" #+ia64 "IA64"` で、**POSIX の
# 名前 (amd64 / aarch64) を書いていなかったため期待値が空になっていた**から。
# 期待値に足した。
#
# **`add-putenv-04` と `test-environ-*` 3 件はここから外れた。** 前は
# 「Win32 の環境変数ブロック」と書いてあったが、**環境変数ブロックは
# 関係が無く、2 つ別の話だった**:
#
#   * `si:putenv` が POSIX で**成功を返して何もしていなかった。**
#     POSIX の `putenv` は渡した文字列を**複写せずポインタを環境に置く**のに、
#     `alloca` で積んだ文字列を渡していた。`setenv` / `unsetenv` に直した
#     (Win32 の `_wputenv` は複写するので、あちらは正しい。**同じ名前で
#     約束が違うものを platform.h で別名にしてはいけなかった。**)
#   * `test-environ-*` の突き合わせ相手が `cmd.exe /c set` だった。
#     POSIX では `env` を使う形にした。**この 2 件が POSIX で測れていれば
#     putenv の壊れ方は捕まっていた** — 「外の目」を Windows 専用にすると、
#     そこで守られるはずの性質が丸ごと測られなくなる。
# `handle-access-violation` / `handle-divide-by-zero` はハードウェア例外を
# win32-exception として受け取るもので、SEH が要る。
# `formatter-*` は外部の整形コマンドを Win32 のプロセス起動で呼ぶ。
#
# **`shell-operation-*` 15 件はここから外れた。** SHFileOperation を呼んで
# いるので「Win32 API を直接呼ぶもの」に分類してあったが、**呼んでいる API が
# Win32 なだけで、やること (複写・移動・削除) は移植できた。** core の
# ファイル操作で組み直した (src/core/pathname.cc の非 Win32 側)。
# 落としたのは進捗ダイアログ・ごみ箱への undo・上書き確認という見た目だけ。
#
# **`uuid-create-*` 8 件もここから外れた。** 前はこのコメントに
# 「CoCreateGuid には uuid_generate があるので移植できる余地がある」と
# 書いてあり、そのとおりだった。**ただし libuuid には依存しなかった** —
# RFC 4122 の中身は「乱数」(version 4) か「時刻 + 機械の識別子」
# (version 1) で、`/dev/urandom` と `clock_gettime` で足りる。Linux ビルドの
# 依存は ncurses と zlib だけに保ちたいので、そちらを選んだ
# (src/core/system.cc の非 Win32 側)。
#
# **この群に残っているものは、名前が Win32 API だからではなく、
# 「その API がやっていること自体が POSIX に無い」から残っている。**
# SEH (ハードウェア例外を Lisp のコンディションとして受け取る) と
# レジストリは無い。分類を書くときはそこを見分けること。

fix-start-timer
formatter-before-save-returns-nil
formatter-run-replaces-on-success
handle-access-violation
handle-access-violation-2
handle-divide-by-zero
last-win32-error
last-win32-error-getlasterror
wait-object-check-type-1
wait-object-typep

# ファイル名とパス。
#
# **理由を測り直した。以前ここには「`chdir` はドライブごとのカレント
# ディレクトリという Win32 の概念に依っている」と書いてあったが、それは
# 違った。** `chdir` 自体は POSIX でも正しく動く:
#
#     (chdir "/tmp/") => t   (cwd) => "/tmp/"
#     (chdir)         => t   (cwd) => ホーム
#
# `chdir-0` から `chdir-6` が落ちるのは、どれも `(get-windows-directory)` と
# `(get-system-directory)` を行き先に使っていて、**POSIX ではこの 2 つが nil を
# 返す**ため (`C:\Windows` に相当するものは無いので、nil が正しい)。
# つまりテストが Windows のディレクトリを前提にしているだけで、実装の不足では
# ない。`chdir-3` は `get-disk-usage`、`chdir-4` は `cmd /c dir` も使う。
#
# **`chdir-path-not-found` はここから外れた。** 前はこのコメントに
# 「POSIX の chdir は ENOENT しか返さないので、区別するには自分で親を辿って
# 確かめるしかない」と書いてあり、**そのとおりだったので、そうした。**
# `file_error (int, lisp)` は**番号とパスの両方を持つ唯一の場所**なので、
# そこで ENOENT のときだけ親を stat して分けている
# (`refine_not_found`、src/core/pathname.cc)。「しかない」で止めずに、
# その手が本当に取れないのかを見ること。
#
#     /no/such/dir/zzz  -> path-not-found  (途中が無い)
#     /tmp/zzz-nope     -> file-not-found  (最後だけ無い)
#
# 1 か所で分けたので chdir だけでなく open や delete-file でも Win32 と
# 同じ条件が付く。
#
# `fix-truename-1` から `-4` は `(get-system-directory)` と `SystemRoot` を
# 使うので上と同じ。`image-startup-option` と
# `fix-*load-pathname*-from-dump-image` はダンプイメージに依っている。
#
# **`user-config-path` / `xyzzy-ini-path` は 7 件のうち 5 件が外れた。** 前は
# 「レジストリと ini ファイルに依っている」と 1 行で片付けてあったが、
# **レジストリが要るのは別の話で、ini ファイルの方は移植できた**
# (issue #143)。848 行あった win32/conf.cc のうち Win32 に触っていたのは
# `Get/WritePrivateProfileStringW` の 2 つだけで、残りは書式の処理だった。
# その 2 つを書き (src/core/ini-posix.cc)、残りを core へ移した
# (src/core/conf-io.cc)。**Linux ビルドはそれまで設定を一切読み書きして
# いなかった。**
#
# **残る 2 件も外れた。** `xyzzy-ini-path-XYZZYINIFILE` と
# `user-config-path-XYZZYCONFIGPATH` は ini とは関係が無く、**環境変数を
# 子プロセスへ渡す方の問題**だった: 非 Win32 の `call-process` /
# `make-process` が `:environ` を丸ごと無視していた (`Kenviron` が
# ncurses-process.cc に出てこなかった)。実装した (`build_environ`)。
# **この 7 件の群は空になった。**
#
# **`directory--*` 5 件はここから外れた。** 前はこのコメントに
# 「WIN32_FIND_DATA の属性 (FILE_ATTRIBUTE_*) をそのまま見ている」と
# 書いてあったが、**5 件のどれも属性を見ていなかった。** 測り直したら
# 別々の 3 つの理由で、全部直せた:
#
#   * `get-file-info` が **ncurses-stubs.cc で nil を返すスタブ**だった。
#     中身は `strict_get_file_data` を呼んで `make_file_info` に渡すだけで、
#     その `WINFS::get_file_data` は非 Win32 でも stat で実装済み。
#     `#ifdef _WIN32` の中にあっただけで、**`directory` の `:file-info t` は
#     同じ `make_file_info` を通って動いていた** (片方だけ空だった)。
#   * `:show-dots t` が `./` と `../` を返さなかった。Win32 の
#     FindFirstFile はこの 2 つを返し、**弾くのは呼び出し側の仕事**
#     (glob.cc の DF_SHOW_DOTS) なのに、vfs-posix.cc が先に落としていた。
#   * `(merge-pathnames "/work/etc" "c:/hoge")` が **"c:/work/etc"** を
#     返していた。ドライブの無い rooted パスに defaults のドライブを被せる
#     のは Win32 では正しいが、POSIX には被せるドライブが無いので嘘になる。
#
# **属性を見ているという理由は、テストを読めば違うと分かるものだった**
# (`directory--absolute` は `merge-pathnames` に `c:/hoge` を渡している)。
# 分類を書くときは、テストが実際に何を呼んでいるかを見ること。
#
# **エラーの番号と文言はここから外した。** POSIX では `GetLastError ()` が
# errno を返すのに `ERROR_*` は Win32 の番号のままで、両者が同じ整数の欄に
# 混ざっている。文言 (src/core/lprint.cc) と `create-directory` の既存判定は
# 直したが、番号の体系そのものを揃える話は別 issue にした。

chdir-0
chdir-1
chdir-2
chdir-3
chdir-4
chdir-5
chdir-6
fix-*load-pathname*-from-dump-image
fix-truename-1
fix-truename-2
fix-truename-3
fix-truename-4
image-startup-option

# コマンドライン引数の処理 — **この群も空になった。**
#
# ここには 26 件 (`process-command-line--*` 25 件と
# `fix (cdr '#1='#1#) printing`) が並んでいて、「POSIX では子プロセスが同じ
# tty を取り合うので測れない」と書いてあった。**原因の見立ては当たっていて、
# 直せば通った。**
#
# どれも自分自身をもう 1 つ起動して引数の効きを見る。子に `--batch` を
# 付けないと、子が親と同じ tty で curses を初期化しようとして、どちらも
# 進まない (結果ファイルが書かれず "result not sent."、`call-process` は 1)。
# コマンド行を組む所を `unittest/simple-test.l` の `test-self-command` に
# まとめ、端末ビルドでは `--batch` を足すようにした。
#
# 2 件だけ別の理由だった: `-e(view-mode)` と `-eval (view-mode)` は
# **括弧がシェルに食われていた。** POSIX では子プロセスを `/bin/sh -c` で
# 起動するので `(` が構文エラーになる (cmd.exe はここでは括弧を特別扱い
# しないので Windows では通っていた)。このファイルの他の指定は最初から
# `-e \"~S\"` の形で引用符に囲まれていて、この 2 つだけが漏れていた。

# ウィンドウの分割と構成、エコー領域 — **この群は空になった。**
#
# ここには 27 件が並んでいて、「`--batch` には画面が無いから仕方ない」と
# 書いてあった。**それが間違いだった。** 原因は画面の有無ではなく、
# `compute_geometry` が `getmaxyx (stdscr, ...)` を直に呼んでいたことである。
# `--batch` では curses を上げないので `stdscr` が 0 で、`getmaxyx` は
# rows も cols も -1 にする。それで `app.active_frame.size` が毎回 -1 に
# 上書きされ、`create_batch_windows` が置いていた 80x24 が消えていた。
# その結果すべてのウィンドウの矩形が潰れ、`split-window` は「分割できません」、
# `window-height` は 1、`screen-height` は -1 を返していた。
#
# **画面が無いのは正しいが、大きさが無いのは正しくない。** 端末の大きさを
# 取る所を 1 箇所 (`term_size`) にまとめ、curses が上がっていなければ
# フレームの持っている大きさを使うようにしたら 23 件が通った。
#
# 残った 4 件のうち 3 件は `window-lines` の別のバグだった: モード行の
# 1 行を一律に引いていて、**モード行を持たないミニバッファでも引いていた。**
# 残り 1 件は `*current-pseudo-frame*` が nil のときの `pseudo-frame-name`。
# どちらも「バッチだから」ではなく、ただのバグだった。
#
# **教訓として残す: 「環境の制約」と書いてある既知失敗は疑う価値がある。**
# 27 件のうち 27 件が環境ではなく実装の問題だった。

# OLE / COM。POSIX には COM が無い。
# 
# ole-tests.l は丸ごと除外してある (tools/run-tests.sh に理由)。ここに残るのは
# 別のファイルから OLE を触っているもの。

fix-ole-method
lisp-mode-eval-last-sexp

# mingw でも落ちるもの。**POSIX 固有ではない。**
# 
# `abbreviate-display-string` (表示用にパスを短くする) は、端末ビルドと
# mingw ビルドが**同じ結果を返し**、MSVC だけが違う。両方とも短縮せずに
# 元の文字列を返す。つまり POSIX の実装不足ではなく、幅の測り方が MSVC と
# 違うということである。misc/known-failures/mingw.txt にも同じ名前があり、
# 理由が分かっていないのもそちらと同じ。

fix-abbreviate-display-string
