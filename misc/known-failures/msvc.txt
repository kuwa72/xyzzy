# Known failures under MSVC (build.yml) on all three architectures.
# See README.md.  Baseline: main @ b918656 (2026-08-21).

# Where xyzzy looks for its ini file and its config directory.  These four go
# together and they fail only under MSVC -- the mingw build passes all four,
# which is worth knowing: it means the tests describe behaviour the code still
# has on one toolchain and not the other, so one of the two is wrong about
# paths.  Worth chasing before the rest of this file.
xyzzy-ini-path
user-config-path-startup-option
user-config-path-XYZZYCONFIGPATH
user-config-path-startup-option-and-XYZZYCONFIGPATH
