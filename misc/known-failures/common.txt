# Known failures on every target, under both toolchains.  See README.md.
#
# Baseline taken from the runs on main @ b918656 (2026-08-21):
#   MSVC     x86 682 tests / 8 failed, x64 and ARM64 680 / 10
#   mingw    i686 675 / 10, x86_64 678 / 10

# OLE: iterating a collection with a for-each style walker.  fix-ole-for-each
# passes under MSVC and fails under mingw; -2 fails everywhere, so the two are
# not the same bug.
fix-ole-for-each-2

# (octet-length "..." :external-format ...) for the non-UTF-16 encodings.  Both
# of these predate the unicode migration and are almost certainly the same bug
# counted twice.
octet-length-eucjp
octet-length-utf8

# HMAC-SHA2 test vector 3 (the long-key case).
#
# This is the failure that had no name.  The mingw x86_64 run reported
#
#   Total 678 tests: 668 passed, 10 failed
#
# while printing only nine "...Failed" lines, so the tenth could not be read out
# of the log at all.  Gating the suite is what produced the name: the first
# gated run called it out as the one unexpected failure.  Worth remembering the
# next time the output and the count disagree.
hmac-sha2-test-case-3
